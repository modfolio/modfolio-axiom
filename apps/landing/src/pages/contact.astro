---
import Base from "~/layouts/Base.astro";
import Header from "~/components/landing/Header.astro";
import Footer from "~/components/landing/Footer.astro";
---

<Base
	title="Contact — Modfolio Axiom"
	description="Modfolio Axiom 연락처 및 사업자 정보."
>
	<Header />
	<main id="main">
		<section class="contact-hero">
			<span class="contact-hero__label">Contact</span>
			<h1 class="contact-hero__title">Contact</h1>
		</section>

		<section class="contact-info section-divider">
			<div class="contact-info__inner">
				<span class="section-label">Contact Information</span>
				<div class="contact-info__grid">
					<div class="info-card">
						<div class="info-card__icon" aria-hidden="true">
							<svg
								width="24"
								height="24"
								viewBox="0 0 24 24"
								fill="none"
								stroke="currentColor"
								stroke-width="2"
								stroke-linecap="round"
								stroke-linejoin="round"
							>
								<rect
									x="2"
									y="4"
									width="20"
									height="16"
									rx="2"></rect>
								<path
									d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"
								></path>
							</svg>
						</div>
						<h3 class="info-card__title">Email</h3>
						<a
							href="mailto:contact@modfolio.io"
							class="info-card__value"
							>contact@modfolio.io</a
						>
					</div>

					<div class="info-card">
						<div class="info-card__icon" aria-hidden="true">
							<svg
								width="24"
								height="24"
								viewBox="0 0 24 24"
								fill="none"
								stroke="currentColor"
								stroke-width="2"
								stroke-linecap="round"
								stroke-linejoin="round"
							>
								<path
									d="M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0Z"
								></path>
								<circle cx="12" cy="10" r="3"></circle>
							</svg>
						</div>
						<h3 class="info-card__title">Address</h3>
						<p class="info-card__value">
							경기도 수원시 팔달구 효원로 278, 6층 602호
						</p>
					</div>
				</div>
			</div>
		</section>

		<section class="contact-business section-divider">
			<div class="contact-business__inner">
				<span class="section-label">Business Information</span>
				<div class="contact-business__content">
					<dl class="contact-business__list">
						<div class="contact-business__item">
							<dt>상호</dt>
							<dd>모드폴리오</dd>
						</div>
						<div class="contact-business__item">
							<dt>대표</dt>
							<dd>김동헌</dd>
						</div>
						<div class="contact-business__item">
							<dt>사업자등록번호</dt>
							<dd>104-95-65636</dd>
						</div>
						<div class="contact-business__item">
							<dt>통신판매업</dt>
							<dd>제2025-수원팔달-0953호</dd>
						</div>
						<div class="contact-business__item">
							<dt>출판사 등록</dt>
							<dd>제2024-000037호</dd>
						</div>
						<div class="contact-business__item">
							<dt>소재지</dt>
							<dd>
								경기도 수원시 팔달구 효원로 278, 6층
								602호
							</dd>
						</div>
					</dl>
				</div>
			</div>
		</section>

		<section class="contact-cta section-divider">
			<a
				href="https://nexus.modfolio.io"
				class="btn btn--primary btn--lg">Explore Portal</a
			>
		</section>
	</main>
	<Footer />
</Base>

<style>
	/* ── Hero ── */
	.contact-hero {
		min-height: 50vh;
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		text-align: center;
		padding-top: var(--header-height);
		padding-inline: clamp(1rem, 4vw, 2rem);
	}

	.contact-hero__label {
		font-family: var(--font-code);
		font-size: var(--text-sm);
		color: var(--color-text-tertiary);
		letter-spacing: var(--tracking-wide);
		text-transform: uppercase;
		margin-bottom: var(--space-5);
	}

	.contact-hero__title {
		font-family: var(--font-display);
		font-size: var(--text-4xl);
		font-weight: 700;
		line-height: var(--leading-tight);
		letter-spacing: var(--tracking-tight);
		/* Fallback: visible text when background-clip:text is unsupported or
		   disabled (forced-colors / high-contrast), where a transparent
		   text-fill would otherwise render the title invisible. */
		color: var(--color-text-primary);
		background: var(--accent-gradient-text);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		margin-bottom: var(--space-5);
	}

	@media (forced-colors: active) {
		.contact-hero__title {
			-webkit-text-fill-color: currentColor;
			background: none;
		}
	}

	/* ── Info Cards ── */
	.contact-info {
		padding-block: var(--space-12);
		padding-inline: clamp(1rem, 4vw, 2rem);
		position: relative;
	}

	.contact-info__inner {
		max-width: var(--content-max-width);
		margin-inline: auto;
	}

	.contact-info__grid {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: var(--space-5);
	}

	.info-card {
		background: var(--card-bg);
		border: var(--card-border);
		border-radius: var(--card-radius);
		padding: var(--card-padding);
		display: flex;
		flex-direction: column;
		gap: var(--space-3);
		transition:
			background var(--dur-base) var(--ease-out),
			box-shadow var(--dur-base) var(--ease-out);
	}

	.info-card:hover {
		background: var(--color-surface-elevated);
		box-shadow: var(--shadow-glow);
	}

	.info-card__icon {
		color: var(--color-text-accent);
		margin-bottom: var(--space-2);
	}

	.info-card__title {
		font-family: var(--font-ui);
		font-size: var(--text-xs);
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: var(--tracking-wide);
		color: var(--color-text-tertiary);
	}

	.info-card__value {
		font-family: var(--font-ui);
		font-size: var(--text-base);
		color: var(--color-text-primary);
		line-height: var(--leading-relaxed);
		text-decoration: none;
		transition: color var(--dur-fast) var(--ease-out);
	}

	a.info-card__value:hover {
		color: var(--color-text-accent);
	}

	/* ── Business Info ── */
	.contact-business {
		padding-block: var(--space-12);
		padding-inline: clamp(1rem, 4vw, 2rem);
		position: relative;
	}

	.contact-business__inner {
		max-width: var(--content-max-width);
		margin-inline: auto;
	}

	.contact-business__list {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: var(--space-5);
	}

	.contact-business__item {
		display: flex;
		flex-direction: column;
		gap: var(--space-1);
	}

	.contact-business__item dt {
		font-family: var(--font-ui);
		font-size: var(--text-xs);
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: var(--tracking-wide);
		color: var(--color-text-tertiary);
	}

	.contact-business__item dd {
		font-family: var(--font-ui);
		font-size: var(--text-base);
		color: var(--color-text-secondary);
		line-height: var(--leading-relaxed);
	}

	/* ── CTA ── */
	.contact-cta {
		text-align: center;
		padding-block: var(--space-12);
		padding-inline: clamp(1rem, 4vw, 2rem);
		display: flex;
		flex-direction: column;
		align-items: center;
		position: relative;
	}

	/* ── Responsive ── */
	@media (max-width: 768px) {
		.contact-info__grid {
			grid-template-columns: 1fr;
		}

		.contact-business__list {
			grid-template-columns: 1fr;
		}
	}
</style>
