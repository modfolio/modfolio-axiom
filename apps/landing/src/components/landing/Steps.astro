<!-- Steps Section — Numbered architecture steps with GSAP scroll reveal -->
<section class="steps" id="steps">
	<div class="steps__inner">
		<span class="section-label">Architecture</span>
		<div class="step">
			<span class="step__number">01</span>
			<div class="step__content">
				<h3 class="step__title">독립 운영</h3>
				<p class="step__body">
					각 서비스는 고유한 목적과 설계를 가집니다. 하나의 장애가 다른
					서비스에 영향을 주지 않는 완전한 독립 구조.
				</p>
			</div>
		</div>
		<div class="step__connector" aria-hidden="true"></div>
		<div class="step">
			<span class="step__number">02</span>
			<div class="step__content">
				<h3 class="step__title">통합 인증</h3>
				<p class="step__body">
					Modfolio Connect 하나의 계정으로 모든 서비스에 접근합니다. 별도
					가입이나 복잡한 연동 과정 없이.
				</p>
			</div>
		</div>
		<div class="step__connector" aria-hidden="true"></div>
		<div class="step">
			<span class="step__number">03</span>
			<div class="step__content">
				<h3 class="step__title">글로벌 인프라</h3>
				<p class="step__body">
					세계 어디서든 동일한 속도와 안정성을 보장합니다. 데이터는
					사용자에게 가장 가까운 곳에서 처리됩니다.
				</p>
			</div>
		</div>
	</div>
</section>

<style>
	.steps {
		padding-block: var(--space-12);
		position: relative;
	}

	.steps::before {
		content: "";
		position: absolute;
		top: var(--space-12);
		left: clamp(1rem, 4vw, 2rem);
		bottom: var(--space-12);
		width: 1px;
		background: linear-gradient(
			180deg,
			transparent,
			var(--color-border-accent) 30%,
			var(--color-border-accent) 70%,
			transparent
		);
		opacity: 0.4;
	}

	.steps__inner {
		max-width: var(--content-max-width);
		margin-inline: auto;
		padding-inline: var(--content-padding);
	}

	.step {
		display: flex;
		gap: var(--space-6);
		align-items: flex-start;
	}

	.step__number {
		font-family: var(--font-code);
		font-size: var(--text-3xl);
		font-weight: 700;
		color: var(--color-text-accent);
		opacity: 0.2;
		min-width: var(--space-9);
		flex-shrink: 0;
	}

	.step__title {
		font-family: var(--font-ui);
		font-size: var(--text-lg);
		font-weight: 700;
		color: var(--color-text-primary);
		margin-bottom: var(--space-2);
	}

	.step__body {
		font-family: var(--font-body);
		font-size: var(--text-base);
		color: var(--color-text-secondary);
		line-height: var(--leading-relaxed);
		max-width: var(--measure);
	}

	.step__connector {
		width: 1px;
		height: var(--space-8);
		border-left: 1px dashed var(--color-border-default);
		margin-left: var(--space-6);
	}
</style>
