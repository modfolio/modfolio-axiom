<!-- Header — layout-patterns 규격: 80px fixed, 전폭, 로고(좌) + nav + CTA(우) -->
<header class="header" id="header">
	<div class="header__inner">
		<a href="/" class="header__brand" aria-label="Modfolio Axiom Home">
			<span class="header__wordmark">Modfolio Axiom</span>
		</a>
		<nav class="header__nav" aria-label="Main navigation">
			<a href="/#focus" class="header__link">Focus</a>
			<a href="/contact" class="header__link">Contact</a>
		</nav>
		<a href="https://nexus.modfolio.io" class="btn btn--primary">Enter Portal</a>
	</div>
</header>

<style>
	.header {
		position: fixed;
		top: 0;
		left: 0;
		right: 0;
		z-index: var(--z-header);
		height: var(--header-height);
		display: flex;
		align-items: center;
		background: var(--header-bg);
		backdrop-filter: blur(12px);
		-webkit-backdrop-filter: blur(12px);
		border-bottom: 1px solid var(--color-border-subtle);
	}

	.header__inner {
		width: 100%;
		display: flex;
		align-items: center;
		justify-content: space-between;
		padding-inline: clamp(1rem, 4vw, 2rem);
	}

	.header__brand {
		display: flex;
		align-items: center;
	}

	.header__wordmark {
		font-family: var(--font-display);
		font-size: var(--text-xl);
		font-weight: 700;
		color: var(--color-text-primary);
		letter-spacing: var(--tracking-tight);
	}

	.header__nav {
		display: flex;
		align-items: center;
		gap: var(--space-6);
	}

	.header__link {
		font-family: var(--font-ui);
		font-size: var(--text-sm);
		color: var(--color-text-tertiary);
		text-decoration: none;
		transition: color var(--dur-fast) var(--ease-out);
	}

	.header__link:hover {
		color: var(--color-text-primary);
	}

	@media (max-width: 640px) {
		.header__nav {
			display: none;
		}
	}
</style>
