<!-- Domains Section — 6-domain grid with hover descriptions -->
<section class="domains" id="domains">
	<div class="domains__inner">
		<span class="section-label">Domains</span>
		<h2 class="domains__heading">Six pillars of applied research</h2>
		<div class="domains__grid">
			<article class="domain-card">
				<span class="domain-card__number">01</span>
				<h3 class="domain-card__title">Engineering</h3>
				<p class="domain-card__desc">
					Infrastructure systems, edge computing, platform architecture
				</p>
			</article>
			<article class="domain-card">
				<span class="domain-card__number">02</span>
				<h3 class="domain-card__title">Science</h3>
				<p class="domain-card__desc">
					Computational research, data modeling, analytical frameworks
				</p>
			</article>
			<article class="domain-card">
				<span class="domain-card__number">03</span>
				<h3 class="domain-card__title">Chemistry</h3>
				<p class="domain-card__desc">
					Molecular analysis, compound databases, lab automation
				</p>
			</article>
			<article class="domain-card">
				<span class="domain-card__number">04</span>
				<h3 class="domain-card__title">Research</h3>
				<p class="domain-card__desc">
					R&D methodology, experimental design, peer collaboration
				</p>
			</article>
			<article class="domain-card">
				<span class="domain-card__number">05</span>
				<h3 class="domain-card__title">Medicine</h3>
				<p class="domain-card__desc">
					Clinical data systems, patient management, diagnostic tools
				</p>
			</article>
			<article class="domain-card">
				<span class="domain-card__number">06</span>
				<h3 class="domain-card__title">Deep Tech</h3>
				<p class="domain-card__desc">
					Incubation programs, frontier technology, applied innovation
				</p>
			</article>
		</div>
	</div>
</section>

<style>
	.domains {
		padding-block: var(--space-12);
		padding-inline: var(--content-padding);
		position: relative;
	}

	.domains::before {
		content: "";
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 1px;
		background: linear-gradient(
			90deg,
			transparent,
			var(--color-border-default) 20%,
			var(--color-border-accent) 50%,
			var(--color-border-default) 80%,
			transparent
		);
	}

	.domains__inner {
		max-width: var(--content-max-width);
		margin-inline: auto;
	}

	.domains__heading {
		font-family: var(--font-display);
		font-size: var(--text-2xl);
		font-weight: 700;
		line-height: var(--leading-snug);
		letter-spacing: var(--tracking-tight);
		color: var(--color-text-primary);
		margin-bottom: var(--space-8);
	}

	.domains__grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: var(--space-5);
	}

	.domain-card {
		background: var(--card-bg);
		border: var(--card-border);
		border-radius: var(--card-radius);
		padding: var(--card-padding);
		min-height: var(--space-12);
		display: flex;
		flex-direction: column;
		transition:
			border-color var(--dur-base) var(--ease-out),
			box-shadow var(--dur-base) var(--ease-out);
	}

	.domain-card:hover {
		border-color: var(--color-border-accent);
		box-shadow: var(--shadow-glow);
		background: var(--color-surface-2);
	}

	.domain-card__number {
		font-family: var(--font-code);
		font-size: var(--text-xs);
		color: var(--color-text-tertiary);
		letter-spacing: var(--tracking-label);
		margin-bottom: var(--space-4);
	}

	.domain-card__title {
		font-family: var(--font-ui);
		font-size: var(--text-lg);
		font-weight: 700;
		color: var(--color-text-primary);
		margin-bottom: var(--space-3);
	}

	.domain-card__desc {
		font-family: var(--font-ui);
		font-size: var(--text-sm);
		color: var(--color-text-tertiary);
		line-height: var(--leading-normal);
		transition: color var(--dur-base) var(--ease-out);
	}

	.domain-card:hover .domain-card__desc {
		color: var(--color-text-secondary);
	}

	@media (max-width: 768px) {
		.domains__grid {
			grid-template-columns: repeat(2, 1fr);
		}
	}
</style>
