<!-- Domains Section — 6-domain grid with hover descriptions -->
<section class="domains" id="domains">
	<div class="domains__inner">
		<span class="section-label">Domains</span>
		<h2 class="domains__heading">Six domains of applied technology</h2>
		<div class="domains__grid">
			<article class="domain-card">
				<span class="domain-card__number">01</span>
				<h3 class="domain-card__title">Defense & Aerospace</h3>
				<p class="domain-card__desc">
					방위 시스템, 위성 통신, 항공우주 기술
				</p>
			</article>
			<article class="domain-card">
				<span class="domain-card__number">02</span>
				<h3 class="domain-card__title">Cybersecurity</h3>
				<p class="domain-card__desc">
					위협 탐지, 암호화 기술, 보안 인프라
				</p>
			</article>
			<article class="domain-card">
				<span class="domain-card__number">03</span>
				<h3 class="domain-card__title">Medical Technology</h3>
				<p class="domain-card__desc">
					의료 기기, 바이오센서, 임상 데이터 플랫폼
				</p>
			</article>
			<article class="domain-card">
				<span class="domain-card__number">04</span>
				<h3 class="domain-card__title">Mobility & Logistics</h3>
				<p class="domain-card__desc">
					자율주행, 실시간 추적, 물류 최적화
				</p>
			</article>
			<article class="domain-card">
				<span class="domain-card__number">05</span>
				<h3 class="domain-card__title">Energy & Materials</h3>
				<p class="domain-card__desc">
					신재생 에너지, 배터리, 신소재 기술
				</p>
			</article>
			<article class="domain-card">
				<span class="domain-card__number">06</span>
				<h3 class="domain-card__title">Quantum & AI</h3>
				<p class="domain-card__desc">
					양자 컴퓨팅, 고급 AI 연산, 차세대 알고리즘
				</p>
			</article>
		</div>
	</div>
</section>

<style>
	.domains {
		padding-block: var(--space-12);
		padding-inline: var(--content-padding);
		position: relative;
	}

	.domains::before {
		content: "";
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 1px;
		background: linear-gradient(
			90deg,
			transparent,
			var(--color-border-default) 20%,
			var(--color-border-accent) 50%,
			var(--color-border-default) 80%,
			transparent
		);
	}

	.domains__inner {
		max-width: var(--content-max-width);
		margin-inline: auto;
	}

	.domains__heading {
		font-family: var(--font-display);
		font-size: var(--text-2xl);
		font-weight: 700;
		line-height: var(--leading-snug);
		letter-spacing: var(--tracking-tight);
		color: var(--color-text-primary);
		margin-bottom: var(--space-8);
	}

	.domains__grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: var(--space-5);
	}

	.domain-card {
		background: var(--card-bg);
		border: var(--card-border);
		border-radius: var(--card-radius);
		padding: var(--card-padding);
		min-height: var(--space-12);
		display: flex;
		flex-direction: column;
		transition:
			border-color var(--dur-base) var(--ease-out),
			box-shadow var(--dur-base) var(--ease-out);
	}

	.domain-card:hover {
		border-color: var(--color-border-accent);
		box-shadow: var(--shadow-glow);
		background: var(--color-surface-2);
	}

	.domain-card__number {
		font-family: var(--font-code);
		font-size: var(--text-xs);
		color: var(--color-text-tertiary);
		letter-spacing: var(--tracking-label);
		margin-bottom: var(--space-4);
	}

	.domain-card__title {
		font-family: var(--font-ui);
		font-size: var(--text-lg);
		font-weight: 700;
		color: var(--color-text-primary);
		margin-bottom: var(--space-3);
	}

	.domain-card__desc {
		font-family: var(--font-ui);
		font-size: var(--text-sm);
		color: var(--color-text-tertiary);
		line-height: var(--leading-normal);
		transition: color var(--dur-base) var(--ease-out);
	}

	.domain-card:hover .domain-card__desc {
		color: var(--color-text-secondary);
	}

	@media (max-width: 768px) {
		.domains__grid {
			grid-template-columns: repeat(2, 1fr);
		}
	}
</style>
