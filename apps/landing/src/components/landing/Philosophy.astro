<!-- Philosophy Section — 2-column "what we see / where we head" -->
<section class="philosophy">
	<div class="philosophy__inner">
		<div class="philosophy__col">
			<span class="philosophy__label">우리가 보는 것</span>
			<p class="philosophy__body">
				방위, 의료, 에너지, 양자. 산업의 가장 깊은 곳에는 아직 누구도 명쾌하게 답하지 못한 문제들이 있습니다.
			</p>
		</div>
		<div class="philosophy__col">
			<span class="philosophy__label">우리가 향하는 곳</span>
			<p class="philosophy__body">
				그 문제 앞에서 기술이 할 수 있는 일을 찾습니다. 먼저 도착하는 것보다, 제대로 도착하는 것에 가치를 둡니다.
			</p>
		</div>
	</div>
</section>

<style>
	.philosophy {
		padding-block: var(--space-12);
	}

	.philosophy__inner {
		max-width: var(--content-max-width);
		margin-inline: auto;
		padding-inline: var(--content-padding);
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: var(--space-10);
	}

	.philosophy__label {
		display: block;
		font-family: var(--font-code);
		font-size: var(--text-xs);
		text-transform: uppercase;
		letter-spacing: var(--tracking-widest);
		color: var(--color-text-accent);
		margin-bottom: var(--space-4);
	}

	.philosophy__body {
		font-family: var(--font-ui);
		font-size: var(--text-lg);
		line-height: var(--leading-relaxed);
		color: var(--color-text-secondary);
	}

	@media (max-width: 768px) {
		.philosophy__inner {
			grid-template-columns: 1fr;
			gap: var(--space-8);
		}
	}
</style>
