<!-- Manifesto Section — GSAP pinned scroll, line-by-line text reveal -->
<section class="manifesto" id="manifesto">
	<div class="manifesto__pin">
		<span class="section-label">Manifesto</span>
		<p class="manifesto__text">
			<span class="manifesto__line">Axiom은 산업의 가장 어려운 문제에 기술로 답합니다.</span>
			<span class="manifesto__line">방위와 항공우주에서 의료와 에너지까지,</span>
			<span class="manifesto__line">사이버 보안에서 양자 컴퓨팅까지 —</span>
			<span class="manifesto__line">불확실한 영역에 확실한 시스템을 구축합니다.</span>
			<span class="manifesto__line">각 서비스는 독립적으로 운영되지만,</span>
			<span class="manifesto__line">하나의 인증과 하나의 약속으로 연결됩니다.</span>
		</p>
	</div>
</section>

<style>
	.manifesto {
		min-height: 100vh;
		position: relative;
		background: radial-gradient(
			ellipse 60% 40% at 50% 50%,
			rgba(129, 140, 248, 0.04),
			transparent
		);
	}

	.manifesto__pin {
		display: flex;
		flex-direction: column;
		justify-content: center;
		min-height: 100vh;
		max-width: var(--content-max-width);
		margin-inline: auto;
		padding-inline: var(--content-padding);
	}

	.manifesto__text {
		font-family: var(--font-body);
		font-size: var(--text-2xl);
		line-height: var(--leading-relaxed);
		max-width: var(--measure);
	}

	.manifesto__line {
		display: block;
		margin-bottom: var(--space-3);
		color: var(--color-text-primary);
	}
</style>
