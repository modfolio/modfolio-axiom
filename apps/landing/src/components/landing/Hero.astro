<!-- Hero Section — WebGL canvas background, minimal content -->
<section class="hero">
	<canvas class="hero__canvas" id="webgl-bg" aria-hidden="true"></canvas>
	<div class="hero__content">
		<span class="hero__eyebrow">Modfolio Axiom</span>
		<h1 class="hero__heading">산업의 깊은 곳에서,<br />더 정밀한 답을 설계합니다.</h1>
		<a href="https://nexus.modfolio.io" class="btn btn--primary btn--lg">Enter Portal</a>
	</div>
</section>

<script>
	import { initMeshGradient } from "~/scripts/webgl-mesh";
	const canvas = document.getElementById("webgl-bg") as HTMLCanvasElement | null;
	if (canvas) initMeshGradient(canvas);
</script>

<style>
	.hero {
		display: flex;
		align-items: center;
		justify-content: center;
		min-height: 100dvh;
		padding-top: var(--header-height);
		position: relative;
		overflow: hidden;
	}

	.hero__canvas {
		position: absolute;
		inset: 0;
		width: 100%;
		height: 100%;
		z-index: var(--z-base);
	}

	.hero__canvas.webgl-fallback {
		background: radial-gradient(
			ellipse 60% 60% at 50% 50%,
			var(--color-surface-1) 0%,
			var(--gray-0) 100%
		);
	}

	.hero__content {
		position: relative;
		z-index: var(--z-elevated);
		max-width: var(--content-max-width);
		text-align: center;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: var(--space-6);
		padding-inline: var(--content-padding);
	}

	.hero__eyebrow {
		font-family: var(--font-code);
		font-size: var(--text-xs);
		text-transform: uppercase;
		letter-spacing: var(--tracking-widest);
		color: var(--color-text-tertiary);
	}

	.hero__heading {
		font-family: var(--font-display);
		font-size: var(--text-5xl);
		font-weight: 700;
		line-height: var(--leading-tight);
		letter-spacing: var(--tracking-tight);
		background: var(--accent-gradient-text);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}
</style>
