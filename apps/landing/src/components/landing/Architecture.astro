<!-- Architecture Section — BAB (Before → After → Bridge) -->
<section class="arch reveal">
	<div class="arch__inner">
		<span class="section-label">Architecture</span>
		<h2 class="arch__heading">어떻게 작동하는가</h2>

		<div class="arch__steps">
			<div class="arch__step">
				<span class="arch__number" aria-hidden="true">01</span>
				<div class="arch__content">
					<span class="arch__step-label">Before</span>
					<p class="arch__body">
						파편화된 모빌리티 시스템. 위치 데이터, 탑승 관리, 사용자 인증이
						각각 다른 서버, 다른 프로토콜로 동작합니다.
					</p>
				</div>
			</div>

			<div class="arch__connector" aria-hidden="true"></div>

			<div class="arch__step">
				<span class="arch__number" aria-hidden="true">02</span>
				<div class="arch__content">
					<span class="arch__step-label">After</span>
					<p class="arch__body">
						Cloudflare Edge에서 통합. SSO 인증 한 번으로 모든 서비스 접근.
						Durable Objects가 실시간 상태를 관리하고 D1이 데이터를 보존합니다.
					</p>
				</div>
			</div>

			<div class="arch__connector" aria-hidden="true"></div>

			<div class="arch__step">
				<span class="arch__number" aria-hidden="true">03</span>
				<div class="arch__content">
					<span class="arch__step-label">Bridge: Axiom</span>
					<p class="arch__body">
						독립 브랜드 앱들이 공통 인프라 위에서 각자의 방식으로 문제를 해결합니다.
						기술 스택의 자유, 운영의 통합.
					</p>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	.arch {
		padding: var(--space-12) var(--content-padding);
		max-width: var(--content-max-width);
		margin: 0 auto;
	}

	.arch__heading {
		font-family: var(--font-display);
		font-size: var(--text-2xl);
		font-weight: 700;
		line-height: var(--leading-snug);
		letter-spacing: var(--tracking-tight);
		color: var(--color-text-primary);
		margin-bottom: var(--space-9);
	}

	.arch__steps {
		display: flex;
		flex-direction: column;
		padding-left: var(--space-4);
	}

	.arch__step {
		display: flex;
		align-items: flex-start;
		gap: var(--space-6);
	}

	.arch__number {
		font-family: var(--font-code);
		font-size: var(--text-3xl);
		font-weight: 700;
		color: var(--color-interactive-primary);
		opacity: 0.2;
		line-height: 1;
		flex-shrink: 0;
		min-width: 60px;
	}

	.arch__content {
		flex: 1;
		max-width: var(--measure);
	}

	.arch__step-label {
		display: block;
		font-family: var(--font-code);
		font-size: var(--text-sm);
		text-transform: uppercase;
		letter-spacing: var(--tracking-widest);
		color: var(--color-interactive-primary);
		margin-bottom: var(--space-2);
	}

	.arch__body {
		font-family: var(--font-body);
		font-size: var(--text-base);
		color: var(--color-text-secondary);
		line-height: var(--leading-relaxed);
	}

	.arch__connector {
		width: 1px;
		height: var(--space-8);
		border-left: 1px dashed var(--color-border-default);
		margin-left: 29px;
	}

	@media (max-width: 768px) {
		.arch__number {
			font-size: var(--text-2xl);
			min-width: 40px;
		}
	}
</style>
