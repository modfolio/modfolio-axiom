<!-- Capabilities Section — FBO (Feature → Benefit → Outcome) -->
<section class="capabilities reveal" id="capabilities">
	<div class="capabilities__inner">
		<span class="section-label">Technology</span>
		<h2 class="capabilities__heading">기술 아키텍처</h2>

		<div class="capabilities__grid">
			<article class="cap-card">
				<div class="cap-card__badge-row">
					<span class="cap-card__badge">EDGE</span>
					<span class="cap-card__dot" aria-hidden="true"></span>
				</div>
				<h3 class="cap-card__title">100% Edge Computing</h3>
				<p class="cap-card__benefit">
					Cloudflare Workers + Pages. 서버리스, 글로벌 엣지에서 실행.
				</p>
				<p class="cap-card__outcome">
					Zero cold start. 어느 국가에서든 동일한 응답 속도.
				</p>
			</article>

			<article class="cap-card">
				<div class="cap-card__badge-row">
					<span class="cap-card__badge">REALTIME</span>
					<span class="cap-card__dot" aria-hidden="true"></span>
				</div>
				<h3 class="cap-card__title">Durable Objects 기반 실시간</h3>
				<p class="cap-card__benefit">
					WebSocket + 상태 유지 오브젝트. 수천 개의 동시 연결을 단일 엔티티로.
				</p>
				<p class="cap-card__outcome">
					셔틀 위치가 1초 이내에 모든 사용자에게 동기화.
				</p>
			</article>

			<article class="cap-card">
				<div class="cap-card__badge-row">
					<span class="cap-card__badge">RESILIENT</span>
					<span class="cap-card__dot" aria-hidden="true"></span>
				</div>
				<h3 class="cap-card__title">독립 스택, 통합 계약</h3>
				<p class="cap-card__benefit">
					각 앱은 고유한 프레임워크와 DB를 선택. SSO와 이벤트 계약으로만 연결.
				</p>
				<p class="cap-card__outcome">
					한 서비스의 장애가 다른 서비스에 전파되지 않음.
				</p>
			</article>
		</div>
	</div>
</section>

<style>
	.capabilities {
		padding: var(--space-12) var(--content-padding);
		max-width: var(--content-max-width);
		margin: 0 auto;
	}

	.capabilities__heading {
		font-family: var(--font-display);
		font-size: var(--text-2xl);
		font-weight: 700;
		line-height: var(--leading-snug);
		letter-spacing: var(--tracking-tight);
		color: var(--color-text-primary);
		margin-bottom: var(--space-8);
	}

	.capabilities__grid {
		display: grid;
		grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
		gap: var(--space-5);
	}

	.cap-card {
		background: var(--color-surface-1);
		border: 1px solid var(--color-border-subtle);
		border-radius: var(--radius-3);
		padding: var(--space-6);
		box-shadow: var(--shadow-1);
		transition:
			transform 0.2s ease,
			border-color 0.2s ease,
			box-shadow 0.2s ease;
	}

	.cap-card:hover {
		transform: translateY(-2px);
		border-color: var(--color-border-hover);
		box-shadow: var(--shadow-2);
	}

	.cap-card__badge-row {
		display: flex;
		align-items: center;
		gap: var(--space-2);
		margin-bottom: var(--space-4);
	}

	.cap-card__badge {
		display: inline-block;
		font-family: var(--font-code);
		font-size: var(--text-xs);
		font-weight: 500;
		letter-spacing: var(--tracking-wide);
		background: var(--color-surface-code);
		color: var(--color-text-code);
		padding: var(--space-1) var(--space-3);
		border-radius: var(--radius-1);
	}

	.cap-card__dot {
		width: 6px;
		height: 6px;
		border-radius: 50%;
		background: var(--color-interactive-secondary);
	}

	.cap-card__title {
		font-family: var(--font-ui);
		font-size: var(--text-lg);
		font-weight: 700;
		color: var(--color-text-primary);
		margin-bottom: var(--space-3);
	}

	.cap-card__benefit {
		font-family: var(--font-ui);
		font-size: var(--text-sm);
		color: var(--color-text-secondary);
		line-height: var(--leading-normal);
		margin-bottom: var(--space-3);
	}

	.cap-card__outcome {
		font-family: var(--font-body);
		font-size: var(--text-sm);
		font-style: italic;
		color: var(--color-interactive-secondary);
		line-height: var(--leading-normal);
	}
</style>
