<!-- Philosophy Section — 2-column "what we see / where we head" -->
<section class="philosophy">
	<div class="philosophy__inner">
		<div class="philosophy__col">
			<span class="philosophy__label">Precision</span>
			<p class="philosophy__body">
				기존의 답이 있지만, 충분하지 않은 곳이 있습니다. 그 간극이 우리가 설계를 시작하는 지점입니다.
			</p>
		</div>
		<div class="philosophy__col">
			<span class="philosophy__label">Rigor</span>
			<p class="philosophy__body">
				속도가 아닌 깊이를 선택합니다. Axiom은 자명한 것부터 다시 묻는 팀입니다.
			</p>
		</div>
	</div>
</section>

<style>
	.philosophy {
		padding-block: var(--section-pad-lg) var(--section-pad-md);
	}

	.philosophy__inner {
		max-width: var(--content-max-width);
		margin-inline: auto;
		padding-inline: var(--content-padding);
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: var(--space-10);
	}

	.philosophy__col::before {
		content: "";
		display: block;
		width: 24px;
		height: 2px;
		background: var(--color-text-accent);
		margin-bottom: var(--space-4);
	}

	.philosophy__label {
		display: block;
		font-family: var(--font-code);
		font-size: var(--text-xs);
		text-transform: uppercase;
		letter-spacing: var(--tracking-widest);
		color: var(--color-text-accent);
		margin-bottom: var(--space-5);
	}

	.philosophy__body {
		font-family: var(--font-ui);
		font-size: var(--text-lg);
		line-height: var(--leading-relaxed);
		color: var(--color-text-secondary);
	}

	@media (max-width: 768px) {
		.philosophy__inner {
			grid-template-columns: 1fr;
			gap: var(--space-8);
		}
	}
</style>
